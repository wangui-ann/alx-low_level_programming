#include "main.h"
#include <string.h>

/**
 * _puts - prints a string to stdout
 * followed by a new line
 * @str: this is the string to be printed
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
