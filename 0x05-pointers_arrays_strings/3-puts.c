#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string to stdout
 * followed by a new line
 * @str: this is the string to be printed
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
