#include "main.h"

/**
 * print_name - function that prints a name
 * @name: name to be printed
 * @p: pointer to thee function
 */

void print_name(char *name, void (*f)(char *))
{
	if (name && p)
		p(name);
}
